{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://127.0.0.1:5210"
      }
    }
  },
  "Daemon": {
    "Url": "http://127.0.0.1:55413/",
    "CookiePath": "/var/lib/blazecoin/.cookie"
  },
  "Index": {
    "DbPath": "data/lite-index.db",
    "PollSeconds": 5,
    "StartHeight": 0
  },
  "Broadcast": {
    "PerMinute": 6
  },
  "Fork": {
    "_comment": "PQ_SIGNATURES 7.1 client update banner. All three stay null until H_Q is chosen; set identically on gw and gw2 the day it is. Name = the fork codename the new lite build lists in SupportedForks (e.g. pqsig), Height = H_Q, MinClientVersion = the first lite build carrying the fork rules (e.g. 2.1.0). Served on /api/status as forkName / forkHeight / minClientVersion.",
    "Name": null,
    "Height": null,
    "MinClientVersion": null
  }
}
