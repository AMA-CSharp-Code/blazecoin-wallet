<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox OFF (2026-09-22): this is a direct download, not a Mac App Store app. A sandboxed
             wallet could not run the bundled blazecoind against the daemon's real datadir
             (~/Library/Application Support/BlazecoinV2.0), and the node listens on TCP 55414 / 55413.
             Turning the sandbox on later means adding network.server + a container-relative datadir. -->
        <key>com.apple.security.app-sandbox</key>
        <false/>
        <key>com.apple.security.network.client</key>
        <true/>
        <key>com.apple.security.network.server</key>
        <true/>
    </dict>
</plist>

